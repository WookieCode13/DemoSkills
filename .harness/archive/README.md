# Harness Archive

This folder contains older Harness pipeline experiments and scratch YAML files that are no longer the primary pipeline definitions.

Archived files:

- `api-ci.yaml`
- `build-demoskills-api-ci.yaml`
- `build-demoskills-api-multi-stage-ci.yaml`
- `simple-testing-stage.yaml`

These are kept for reference because they capture earlier pipeline ideas and experiments.
